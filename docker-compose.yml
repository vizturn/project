services:
  api:
    build:
      context: ./permit-api
      dockerfile: Dockerfile
    image: permit-api/php83
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    ports:
      - '${APP_PORT:-80}:80'
    volumes:
      - './permit-api:/var/www/html'
      - 'api-vendor:/var/www/html/vendor'
    networks: [permit]
    depends_on:
      mysql:
        condition: service_healthy

  # Penjadwal tugas — menjalankan perintah terjadwal yang terdaftar pada
  # routes/console.php, yaitu pemeriksaan masa berlaku izin dan pengiriman
  # rekapitulasi mingguan kepada tim SHE.
  #
  # Memakai citra yang sama dengan layanan api, namun proses utamanya adalah
  # `schedule:work` sehingga berjalan terus-menerus dan memeriksa jadwal setiap
  # menit. Layanan ini tidak membuka porta karena tidak melayani permintaan.
  scheduler:
    build:
      context: ./permit-api
      dockerfile: Dockerfile
    image: permit-api/php83
    command: php artisan schedule:work
    volumes:
      - './permit-api:/var/www/html'
      - 'api-vendor:/var/www/html/vendor'
    networks: [permit]
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy

  web:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - '5173:5173'
    volumes:
      - './permit-web:/app'
      - '/app/node_modules'
    networks: [permit]

  mysql:
    image: 'mysql/mysql-server:8.0'
    ports:
      - '${FORWARD_DB_PORT:-3306}:3306'
    environment:
      MYSQL_ROOT_PASSWORD: '${DB_PASSWORD:-password}'
      MYSQL_ROOT_HOST: '%'
      MYSQL_DATABASE: '${DB_DATABASE:-permit_she}'
      MYSQL_USER: '${DB_USERNAME:-sail}'
      MYSQL_PASSWORD: '${DB_PASSWORD:-password}'
    volumes:
      - 'permit-mysql:/var/lib/mysql'
      # Skrip yang dijalankan sekali saat basis data pertama kali dibentuk.
      # Membuat basis data khusus pengujian otomatis (permit_she_test).
      - './docker/mysql/init-test-db.sql:/docker-entrypoint-initdb.d/10-init-test-db.sql:ro'
    networks: [permit]
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-p${DB_PASSWORD:-password}"]
      retries: 3
      timeout: 5s

  # Mailpit — SMTP server + web UI untuk menguji email di lokal (pengganti Mailtrap).
  # SMTP di port 1025 (dipakai Laravel), web UI di http://localhost:8025 (lihat email).
  mailpit:
    image: 'axllent/mailpit:latest'
    ports:
      - '${FORWARD_MAILPIT_PORT:-1025}:1025'
      - '${FORWARD_MAILPIT_DASHBOARD_PORT:-8025}:8025'
    networks: [permit]

networks:
  permit:
    driver: bridge

volumes:
  permit-mysql:
    driver: local
  api-vendor:
    driver: local
